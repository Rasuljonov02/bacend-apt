export namespace Auth {
    export interface Register {
        email: string;
        password: string;
        name: string;
    }

    export interface logIn {
        email: string;
        password: string;


    }
}

export namespace QuizApp{
    export interface savol {
        name: string;
        answer: string;
        a: string;
        b: string;
        c: string;
    }


}

